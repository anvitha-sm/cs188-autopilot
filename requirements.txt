# Pin every dependency so the simulation produces identical behaviour
# across machines and over time.
#
# Install with:
#   pip install -r requirements.txt
#
# Webots itself must be installed separately from https://cyberbotics.com.
# The Webots Python bindings (controller) are provided by the Webots
# installation and do not need to be listed here.

mediapipe==0.10.14
opencv-python==4.10.0.84
numpy==1.26.4
SpeechRecognition==3.10.4

# PyAudio is the default audio back-end used by SpeechRecognition.
# On Linux install portaudio19-dev first:  sudo apt install portaudio19-dev
# On macOS:  brew install portaudio
# On Windows the wheel ships with everything needed.
PyAudio==0.2.14
